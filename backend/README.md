# Identity Hub - Backend

The backend for Identity Hub provides API endpoints for user enrollment, authentication, card management, and transaction processing.

## Setup

1. `cd backend`
2. `npm install`
3. `cp .env.example .env`
4. `npm start`
