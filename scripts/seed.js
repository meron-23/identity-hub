// Seed script for Identity Hub database

const seedData = async () => {
  console.log('Seeding mock data for Identity Hub...');
  // Logic to populate MongoDB with initial mock users and cards
};

seedData();
