# Identity Hub - Frontend

The frontend for Identity Hub is a React application that allows users to enroll with biometrics, manage their cards, and make secure payments.

## Setup

1. `cd frontend`
2. `npm install`
3. `npm start`
