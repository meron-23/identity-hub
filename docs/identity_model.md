# Identity & Authentication Model

Identity Hub uses Decentralized Identifiers (DIDs) for user identity management.

## Authentication Process

1. User registers with face scan and physical ID.
2. AI extracts ID data (OCR) and face embeddings.
3. System generates a unique DID.
4. Future logins use face biometric matching against the stored DID embedding.
